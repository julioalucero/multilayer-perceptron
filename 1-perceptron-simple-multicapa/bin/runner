#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'perceptronSimple'

nInputs = 9
file_path = 'prueba.csv'
csv = PerceptorSimple::Csv.new(file_path)
entrenamiento = PerceptorSimple::TrainingPS.new(nInputs)
entrenamiento.test(csv.matrix)
