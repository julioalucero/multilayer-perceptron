#!/usr/bin/env ruby
$LOAD_PATH.unshift File.expand_path('../../lib', __FILE__)

require 'rbfSom'

#prueba 1D
v = []

60.times do
 v << [-1 + rand * 2, -1 + rand * 2, rand > 0.5 ? 1 : -1]
end

patrones = v

#som = Perceptron::Som.new(v, 1 , 2 ,0.9 ,50)
#som.entrenamiento

#prueba 2D

# TODO ¿esto no se usa?
cant = 50 * 50

#paso = 2.0 / 50.0
#aux = []
#i = 0
#
#50.times do
#  aux << -1 + i * paso
#  i += 1
#end
##p "que es aux: "
##p aux
#
#patrones = []
#for i in 0...aux.count
#  for k in 0...aux.count
#    patrones << [aux[i], aux[k]]
#  end
#end

epocas_globales    = 7
epocas_transicion  = 1
epocas_ajuste_fino = 3

epocas = [epocas_globales, epocas_transicion, epocas_ajuste_fino]

som = RbfSom::Som.new(patrones, 4 ,4 ,0.9 , epocas)
som.entrenamiento
